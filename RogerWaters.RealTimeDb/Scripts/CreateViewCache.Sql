IF OBJECT_ID('{cacheName}') IS NOT NULL
BEGIN
	DROP TABLE {cacheName}
END

SELECT *
INTO {cacheName}
FROM {viewName}

ALTER TABLE {cacheName} ADD CONSTRAINT
	PK_{cacheName.Schema}_{cacheName.Name} PRIMARY KEY CLUSTERED   
	(
		{primaryColumnList}
	) WITH( STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY];
ALTER TABLE {cacheName} SET (LOCK_ESCALATION = DISABLE);